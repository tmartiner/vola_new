name: models
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: ">=3.2.3 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  built_value: ^8.8.1
  equatable: ^2.0.5

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.7
  built_value_generator: ^8.8.1

flutter:
